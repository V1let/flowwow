# Отчет по тестированию безопасности

Дата: 20.02.2026  
Проект: Flowwow

## 1. Проверка авторизации

**Сценарии:**
1. Вход под обычным пользователем — **Pass**
2. Вход с неверным паролем — **Pass** (ожидаемая ошибка)
3. Выход из системы — **Pass** (клиентская очистка JWT в `localStorage`)

## 2. Проверка разграничения прав доступа

**Сценарии:**
1. Доступ пользователя к `/api/admin/*` — **Pass** (запрещено)
2. Доступ администратора к `/api/admin/*` — **Pass**
3. Доступ без токена к `/api/admin/*` — **Pass** (запрещено)

## 3. Проверка на уязвимости

**Сценарии:**
1. Попытка обхода авторизации (без токена) — **Pass**
2. Некорректный ввод данных в регистрации — **Pass**  
   Обработка возвращает ошибку без утечки служебной информации.

## 4. Выявленные несоответствия

Не выявлено.

## 5. Итог

Авторизация и разграничение прав работают корректно.  
Проверки на базовые попытки обхода и некорректный ввод пройдены.
